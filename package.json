{
  "name": "social-network-mock",
  "version": "0.0.2",
  "description": "social network mock for data structures in memory",
  "main": "dist/social-network-mock.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepare": "tsc --project tsconfig.json && tscpaths -p tsconfig.json -s ./src -o ./dist"
  },
  "author": "Jeff Mendez",
  "license": "MIT",
  "devDependencies": {
    "jest": "^26.4.0",
    "ts-jest": "^26.4.1",
    "tscpaths": "0.0.9",
    "typescript": "^3.7.5"
  },
  "jest": {
    "preset": "ts-jest",
    "moduleNameMapper": {
      "^@app/(.*)$": "<rootDir>/src/$1"
    }
  }
}
